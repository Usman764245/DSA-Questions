#include <iostream>
using namespace std;
// predict time complexity
int main()
{
    int n;
    for (int i = 0; i < n; i++) // n times loop run
    {
        for (int j = 0; j < i; j++) // i times loop run
        {
            // constant work done
        }

        // time complexity O(n^2)    
         }
        return 0;
    }